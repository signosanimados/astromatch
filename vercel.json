{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "vite",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- ':!backend/**'",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
